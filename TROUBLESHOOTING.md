# 🔧 إصلاح الشاشة البيضاء - Quick Fix

## المشكلة المحتملة

عند إنشاء `GiftServiceV2.ts`، قد يكون هناك خطأ في TypeScript أو استيراد خاطئ.

## الحل السريع ⚡

### الخطوة 1: أعد تشغيل dev server
```bash
# أوقف السيرفر (Ctrl+C في Terminal)
# ثم شغله من جديد
pnpm dev
```

### الخطوة 2: احذف cache
```bash
# في PowerShell
Remove-Item -Path "node_modules/.vite" -Recurse -Force
pnpm dev
```

### الخطوة 3: تحقق من Console
1. افتح المتصفح (http://localhost:8080)
2. اضغط **F12** (Dev Tools)
3. اذهب لـ **Console** tab
4. ابحث عن أخطاء حمراء

## الأخطاء الشائعة وحلولها

### خطأ: "Cannot find module"
**الحل:**
```bash
pnpm install
```

### خطأ: "Unexpected token"
**الحل:** تحقق من صيغة JSON في:
- `package.json`
- `tsconfig.json`
- `.env`

### خطأ: "Failed to fetch"
**الحل:** تأكد من:
- السيرفر شغال
- البورت 8080 مفتوح
- لا يوجد Firewall يحجب

## إذا استمرت المشكلة

### حذف كل Cache وإعادة البناء:
```bash
# حذف كل شيء
Remove-Item -Path "node_modules" -Recurse -Force
Remove-Item -Path "pnpm-lock.yaml" -Force
Remove-Item -Path ".vite" -Recurse -Force -ErrorAction SilentlyContinue

# إعادة التثبيت
pnpm install

# تشغيل
pnpm dev
```

## الملفات التي أُنشئت (قد تسبب مشكلة)

إذا كانت المشكلة من الملفات الجديدة، يمكن حذف:
```
src/services/AuthServiceV2.ts
src/services/GiftServiceV2.ts
src/types/database.types.ts
```

**ملاحظة:** هذه الملفات اختيارية للـ Backend الجديد. التطبيق يعمل بدونها.

## استعادة النسخة السابقة

إذا أردت العودة للنسخة التي كانت تعمل:

```bash
# في Git
git status
git restore .
```

أو:
```bash
# استرجاع ملف معين
git restore src/services/GiftServiceV2.ts
```

## التحقق من التطبيق يعمل

افتح المتصفح وجرّب:
1. http://localhost:8080 (الصفحة الرئيسية)
2. سجّل دخول بـ `admin` / `admin123`
3. جرّب التنقل بين الصفحات

إذا ظهرت الصفحة الرئيسية → **التطبيق يعمل ✅**
إذا ظهرت شاشة بيضاء → **تحقق من Console للأخطاء**

---

**آخر تحديث:** 2025-12-07
