# خاصية التتبع والانضمام للغرفة

## نظرة عامة
تم إضافة خاصية التتبع التي تسمح للمستخدمين بمتابعة مستخدمين آخرين والانضمام تلقائياً إلى الغرف الصوتية التي يتواجدون فيها.

## المميزات الرئيسية

### 1. **تتبع حالة المستخدم**
- عرض حالة المستخدم (متصل / في غرفة / غير متصل)
- مؤشر بصري على الصورة الشخصية:
  - 🟢 نقطة خضراء: المستخدم متصل
  - 📡 شارة "في غرفة": المستخدم في غرفة صوتية

### 2. **زر الانضمام للغرفة**
- زر ديناميكي يعرض اسم الغرفة عند تواجد المستخدم فيها
- انتقال تلقائي للغرفة عند الضغط
- رسالة خطأ إذا كان المستخدم غير موجود في غرفة

### 3. **زر المتابعة**
- متابعة/إلغاء متابعة المستخدمين
- أيقونة قلب مع تأثيرات بصرية
- رسائل تأكيد عند المتابعة/إلغاء المتابعة

## الملفات المضافة/المعدلة

### الملفات الجديدة:
1. **`src/services/UserPresenceService.ts`**
   - Service لإدارة حالة المستخدمين
   - تتبع الغرف الموجود فيها المستخدمين
   - إدارة الحالات (متصل/غير متصل/في غرفة)

2. **`src/utils/demoData.ts`**
   - بيانات تجريبية لاختبار الخاصية
   - تهيئة مستخدمين في غرف مختلفة

### الملفات المعدلة:
1. **`src/pages/profile/ModernProfile.tsx`**
   - إضافة أزرار المتابعة والانضمام للغرفة
   - مؤشر الحالة على الصورة الشخصية
   - دوال handleFollowUser و handleToggleFollow

2. **`src/components/voice/VoiceChatRoomRedesign.tsx`**
   - تسجيل المستخدم عند دخول الغرفة
   - إزالة المستخدم عند مغادرة الغرفة

3. **`src/pages/Index.tsx`**
   - تهيئة البيانات التجريبية عند تحميل التطبيق

## كيفية الاستخدام

### 1. للمستخدمين:
1. افتح ملف شخصي لمستخدم آخر
2. شاهد حالة المستخدم (متصل أو في غرفة)
3. اضغط "متابعة" لمتابعة المستخدم
4. اضغط "انضم لغرفته" للانضمام إلى الغرفة الموجود فيها

### 2. للتطوير:
```typescript
import { UserPresenceService } from '@/services/UserPresenceService';

// تسجيل مستخدم في غرفة
UserPresenceService.setUserInRoom('userId', 'roomId', 'Room Title');

// التحقق من حالة المستخدم
const userRoom = UserPresenceService.getUserCurrentRoom('userId');
const status = UserPresenceService.getUserStatus('userId');

// إزالة المستخدم من الغرفة
UserPresenceService.removeUserFromRoom('userId');
```

## البيانات التجريبية

تم تهيئة 3 مستخدمين تجريبيين:
- **user123**: في غرفة "وكالة (Batman)" (r1)
- **user456**: في غرفة "وكالة أسود حلب" (r2)
- **user789**: في غرفة "وكالة تعز الحالمة" (r3)

لاختبار الخاصية:
1. افتح البروفايل بـ userId: `/profile/user123`
2. ستشاهد مؤشر "في غرفة"
3. اضغط "انضم لغرفته" للانتقال للغرفة

## واجهة المستخدم

### الأزرار:
- **زر المتابعة**: 
  - لون أرجواني-وردي عند عدم المتابعة
  - لون رمادي مع قلب أحمر عند المتابعة
  
- **زر الانضمام**:
  - لون أزرق-سماوي مع نص "انضم لغرفته"
  - يعرض اسم الغرفة عند التواجد
  - معطل (رمادي) عند عدم التواجد في غرفة
  - أيقونة Radio متحركة عند التواجد في غرفة

### المؤشرات:
- **نقطة خضراء**: المستخدم متصل
- **شارة "في غرفة"**: المستخدم في غرفة صوتية (خضراء مع أيقونة Radio)

## الخطوات القادمة (TODO)

### Backend Integration:
- [ ] ربط UserPresenceService بـ Supabase
- [ ] Real-time updates عند دخول/مغادرة الغرف
- [ ] حفظ قائمة المتابعين في قاعدة البيانات
- [ ] إشعارات عند دخول المتابَع إلى غرفة

### تحسينات الواجهة:
- [ ] رسوم متحركة عند الانتقال للغرفة
- [ ] قائمة المتابعين المتصلين حالياً
- [ ] تصفية حسب المتابعين الموجودين في غرف

### الأمان:
- [ ] التحقق من صلاحيات الانضمام للغرفة
- [ ] حد أقصى لعدد المتابعين
- [ ] منع spam المتابعة

## الاختبار

### اختبار يدوي:
1. شغل التطبيق: `pnpm dev`
2. انتقل للصفحة الرئيسية (يتم تهيئة البيانات التجريبية)
3. افتح `/profile/user123`
4. تأكد من ظهور مؤشر "في غرفة"
5. اضغط "انضم لغرفته"
6. تأكد من الانتقال لغرفة Batman

### اختبار برمجي:
```typescript
// في console المتصفح:
import { UserPresenceService } from '@/services/UserPresenceService';

// التحقق من وجود المستخدمين
console.log(UserPresenceService.getUserCurrentRoom('user123'));
// Expected: { roomId: 'r1', roomTitle: 'وكالة (Batman)' }

console.log(UserPresenceService.getUserStatus('user123'));
// Expected: 'in-room'
```

## الدعم الفني

للأسئلة أو المشاكل، راجع:
- كود UserPresenceService: `src/services/UserPresenceService.ts`
- كود البروفايل: `src/pages/profile/ModernProfile.tsx`
- البيانات التجريبية: `src/utils/demoData.ts`
