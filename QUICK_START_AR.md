# 🚀 تفعيل الوضع الحقيقي (Production Mode Setup)

## الخطوات السريعة (Quick Steps)

### 1️⃣ سجل حساب مستخدم
```
افتح: http://localhost:8080/auth/register
سجل حساب جديد بالإيميل والباسورد
```

### 2️⃣ احصل على User ID
```
1. افتح Supabase Dashboard
2. انتقل إلى Table Editor → users
3. انسخ ID (UUID) للمستخدم الذي سجلته
```

### 3️⃣ أنشئ الغرف الصوتية
```
1. افتح: supabase/insert_test_rooms.sql
2. استبدل 'YOUR_USER_ID_HERE' بـ UUID المستخدم
3. نفذ السكريبت في Supabase SQL Editor
```

### 4️⃣ اختبر مع صديق
```
1. سجل حساب ثاني (صديقك)
2. افتح التطبيق من الحسابين
3. افتح نفس الغرفة
4. اجلس على مقعد
5. فعّل الميكروفون
6. تحدث واستمع!
```

## ✅ التغييرات المكتملة

- ✅ إزالة جميع البيانات التجريبية من الغرف الصوتية
- ✅ تعطيل enableDemoData
- ✅ تحديث roomService ليستخدم Supabase
- ✅ تحديث AdvancedSearchService ليستخدم Supabase
- ✅ إضافة تحميل المقاعد من Supabase
- ✅ Real-time messaging جاهز
- ✅ TRTC Voice Chat مُفعّل

## 📋 ملفات SQL جاهزة

1. **supabase/schema.sql** - قاعدة البيانات الكاملة (مطبقة ✅)
2. **supabase/migration_add_missing_user_fields.sql** - الحقول الإضافية (مطبقة ✅)
3. **supabase/insert_test_rooms.sql** - الغرف التجريبية (جاهزة للتطبيق ⏳)

## 🔧 التحقق من الإعدادات

افتح Developer Console (F12) وتأكد من:

```javascript
// يجب أن ترى:
✅ Setting up Realtime subscriptions for room: room-1
✅ Messages channel status: SUBSCRIBED
✅ TRTC: Join success
✅ Loaded N active rooms from Supabase

// يجب ألا ترى:
❌ Supabase not ready
❌ No active rooms found
❌ TRTC: Join failed
```

## 🐛 حل المشاكل السريع

### الغرف لا تظهر؟
```sql
-- تحقق من وجود الغرف
SELECT * FROM voice_rooms WHERE is_active = true;

-- إذا لم توجد، نفذ insert_test_rooms.sql
```

### المستخدمون يظهرون على أكثر من مقعد؟
```sql
-- احذف المقاعد المكررة
DELETE FROM voice_room_seats WHERE room_id = 'room-1';
```

### الصوت لا يعمل؟
```
1. تحقق من إذن الميكروفون في المتصفح
2. تحقق من TRTC_SDK_APP_ID في .env
3. افتح Developer Console وابحث عن أخطاء TRTC
```

## 📖 للمزيد من التفاصيل

اقرأ: **PRODUCTION_MODE_GUIDE.md** (دليل شامل بالعربي)

---

**جاهز للاختبار؟** ابدأ الآن! 🎉
